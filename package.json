{
  "name": "abu-talin-electric-plumbing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate": "node scripts/generate-services.mjs",
    "check": "node scripts/check-site.mjs",
    "build": "npm run generate && npm run check && node scripts/build.mjs"
  }
}
